chrome.runtime.onInstalled.addListener(() => {
    console.log("Back Button Locker Extension Installed.");
});

// background.js is currently a placeholder for potential future logic
// like detecting incognito tabs if needed.
